node_modules
dist
.env
.env.local
.git
.github
.vscode
infra
*.md
.postgres-data
.mailpit-data
# data/ is regenerated at build time by `npm run osm:fetch`. Excluding it
# from the build context guarantees a deterministic fetch (a dev's stale
# local cache can't sneak into the image) and keeps the upload small.
data
