node_modules
dist
.env
.env.local
.git
.github
.vscode
infra
*.md
.postgres-data
.mailpit-data
# Dev caches under data/ stay out of the build context, but the CI
# pre-fetched OSM bundle must be included so the in-Docker osm:fetch
# cache-fresh check skips the Overpass call.
data/*
!data/osm-routes/
data/osm-routes/*
!data/osm-routes/*.geojson
